# TASK-20260705-02: Row-level re-capture of Q07/Q09 for R2 report visuals

GOAL: Replace TASK-20260704-05's two disclosed R2 substitute visuals (top-5-by-units bar
in place of a monthly trend; catalog-totals panel in place of a price-vs-cost scatter) with
the row-level data the report-spec's R2 visuals actually call for, by capturing the full
output of `Q07_r2_monthly_units_top5_categories.sql` and `Q09_r2_price_vs_cost.sql` instead
of the aggregate reproducibility signature currently on file.

CONTEXT: IDX-006 (report spec — R2 trend + price-vs-cost scatter), IDX-014 (gold layer,
`medallion/c_gold/queries/Q07*.sql` and `Q09*.sql` — both already written and already run
at full grain; only the *captured* output is an aggregate signature, per medallion-spec
§Reproducibility rule 4), IDX-015 (report deliverable + its disclosed escalations).

DEFINITIONS REQUIRED: none new. DEF-003, DEF-004 (Q07) and RULE-008 (Q09) are unchanged —
this task changes what's captured, not the SQL or any business logic.

DELIVERABLE: `medallion/c_gold/EXPECTED_OUTPUTS.md` updated in place — replace the Q07 and
Q09 aggregate-signature entries with their full row-level `--batch` output (~450 rows for
Q07: 90 months × 5 categories; 850 rows for Q09: one per product). Drop the "captured as an
aggregate signature" note from each query's header comment once the full capture exists.
Do not touch the SQL logic in Q07.sql/Q09.sql themselves — they already produce the right
grain; this is a capture-only amendment, not a gold-layer change.

DONE MEANS:
- Q07 full output pasted verbatim, row count = 450 (90 × 5), deterministic order
  (sales_month, category_id per its existing ORDER BY).
- Q09 full output pasted verbatim, row count = 850 (= dim_product / B01 products count),
  deterministic order (product_id per its existing ORDER BY).
- Regression check: SUM(units_sold) and SUM(gross_revenue) across the new 450 Q07 rows must
  equal the totals in the previously captured aggregate signature; Q09's below-cost count
  (17) and any catalog $ totals must equal the previously captured signature — zero drift.

CONSTRAINTS: Requires live DB access per `process/mysql-setup.md` (Windows local MySQL,
`oakhaven_gold` schema) — **this task cannot be executed in a session without DB
connectivity.** Read-only: no schema/view changes, no writes.

QA PLAN: Validator diffs the new row counts and independently re-derives 2–3 spot totals
from the full captures against the old aggregate signature to confirm no drift before Ian
reviews.

OWNER: sql-builder → sql-validator → Ian
