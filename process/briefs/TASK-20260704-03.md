# TASK-20260704-03: Silver layer (oakhaven_silver views)
GOAL: A complete cleaned/typed/conformed view layer, 1:1 with bronze, provably lossless.
CONTEXT: IDX-005 (§Silver rules), IDX-003 (all cleaning DEFs), IDX-004; TASK-02 captures (reconciliation targets)
DEFINITIONS REQUIRED: DEF-009…DEF-017 — **APPROVED versions** (v1.0). BLOCKED until Ian approves the registry and the DEF-012/013 mapping tables (populated from TASK-02's B03 census) land.
DELIVERABLE: `outputs/TASK-20260704-03/silver/ddl/` (S00 create schema + one file per view, all 13 views per medallion-spec naming) + `silver/verify/` verification queries + `EXPECTED_OUTPUTS.md`.
DONE MEANS: Row count of every silver view = bronze table (RULE-005); zero unmapped-value NULL leaks for DEF-009/012/013 transforms; sentinel flag counts = B04 census numbers; DEF-014 resolution accounts for exactly 150 candidates; every lossy transform keeps <name>_raw; validator re-run of all verify queries matches captures.
CONSTRAINTS: CREATE SCHEMA/VIEW in oakhaven_silver only. No data copies, no base tables. Planted anomalies pass through untouched (RULE-008).
QA PLAN: Ian runs V01 row-parity and one before/after sample query (e.g., 10 customers' phone bronze vs silver).
OWNER: sql-builder → sql-validator → Ian
