# TASK-20260704-05: Report design templates + 3 exploratory reports
GOAL: A reusable exploratory-report template and three working reports (R1 Sales Explorer, R2 Product & Category Explorer, R3 Data Quality Explorer) whose every number traces to a gold/bronze query.
CONTEXT: IDX-006 (report spec — normative), IDX-003 (metric DEFs), TASK-04 gold queries + captures
DEFINITIONS REQUIRED: DEF-004…DEF-008, DEF-018 (display metrics)
DELIVERABLE: `outputs/TASK-20260704-05/reports/` — `template.html` (annotated skeleton implementing report-spec anatomy) + `R1_sales_explorer.html`, `R2_product_explorer.html`, `R3_data_quality.html`, each self-contained (inline CSS/JS, data embedded as JSON generated from captured query outputs) with the required lineage footer.
DONE MEANS: report-spec acceptance criteria 1–4 met per report; KPI-row numbers diff-checked by validator against gold captures; annotations for seasonality/COVID dip present in R1; anomaly callouts (D16/D17) present in R2; every D-pattern appears in R3's census visual.
CONSTRAINTS: Load the `dataviz` skill before writing chart code. No external requests, no CDNs. Data volume per report kept to what the marts emit (pre-aggregated) — never embed raw fact rows.
QA PLAN: Ian opens each file from disk, checks console is clean, and traces two KPI numbers through the lineage table to EXPECTED_OUTPUTS.md.
OWNER: report-designer → sql-validator (lineage/number check) → Ian
