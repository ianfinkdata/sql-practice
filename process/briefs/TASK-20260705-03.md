# TASK-20260705-03: Wire R2's real Q07/Q09 row-level data (retire the disclosed substitutes)

GOAL: TASK-20260704-05's R2 Product & Category Explorer used two disclosed substitute
visuals because Q07/Q09 only had aggregate-signature captures at the time. TASK-20260705-02
has since captured both at full row-level grain (450 rows / 850 rows) in
`medallion/c_gold/EXPECTED_OUTPUTS.md`. Replace the two substitutes with the report-spec's
actual visuals and remove the now-resolved escalation notes.

CONTEXT: IDX-006 (report spec — R2 trend + price-vs-cost scatter), IDX-014/IDX-015 (gold
layer + report deliverable), `outputs/TASK-20260704-05/reports/R2_product_explorer.html`
(current file — read it in full first, including its embedded JSON and lineage table),
`medallion/c_gold/EXPECTED_OUTPUTS.md` §Q07_r2_monthly_units_top5_categories.sql (line ~472,
450 rows: sales_month, category_id, category_name, units_sold, gross_revenue) and
§Q09_r2_price_vs_cost.sql (line ~971, 850 rows: product_id, sku, category_name, unit_cost,
list_price, is_below_cost).

DEFINITIONS REQUIRED: none new. DEF-003, DEF-004 (Q07); RULE-008 (Q09 below-cost flag) —
unchanged, already cited correctly elsewhere in the report.

DELIVERABLE: Amend `outputs/TASK-20260704-05/reports/R2_product_explorer.html` in place:
- Replace the Q08-leaf substitute bar with the report-spec's actual "monthly units by
  top-5 category" trend (line or small-multiple), sourced from the full Q07 capture (450
  rows, 90 months x 5 categories).
- Replace the catalog-totals-panel substitute with the report-spec's actual price-vs-cost
  scatter (850 points), sourced from the full Q09 capture, with the D16 below-cost points
  visually highlighted (diverging/callout color per dataviz skill — load it again before
  touching chart code).
- Remove both `.escalation` boxes and the "Known data gaps" section (or repurpose it to note
  the gap is now resolved, if any other genuine gap remains — check before deleting outright).
- Update the lineage table rows for these two visuals: drop "(aggregate signature only)",
  point at the real row-level capture reference.
- Leave every other visual/KPI/table in R2 untouched — this is a scoped swap of exactly two
  visuals, not a rebuild.

CONSTRAINTS: Data volume: embed only what's needed for these two visuals (450 + 850 rows is
fine per report-spec's "pre-aggregated, no raw fact rows" rule — these ARE the pre-aggregated
mart/dim rows, not raw fact table rows). No CDNs, no external requests. Don't touch R1,
R3, template.html, or MANIFEST.md's already-logged validator findings (append, don't rewrite,
if MANIFEST needs updating to reflect the fix).

DONE MEANS: Both new visuals render from the real captured data (spot-checkable against
EXPECTED_OUTPUTS.md); no escalation boxes remain for Q07/Q09; lineage table accurate; file
still self-contained (grep clean for CDN/fetch/external refs); acceptance criteria 1-4 from
report-spec still met.

OWNER: report-designer → sql-validator → Ian
